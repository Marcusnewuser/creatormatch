# Supabase (required)
# Dashboard → Project Settings → API → Project URL (no /rest/v1 at the end)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# Dashboard → Project Settings → API → anon public key
VITE_SUPABASE_ANON_KEY=your-anon-key
